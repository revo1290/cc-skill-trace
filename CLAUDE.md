# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Status

This repository is in its initial state. No source files, build configuration, or tests have been added yet. The `.gitignore` is configured for a Node.js project (ignores `node_modules/`, `dist/`, `.env`, `.env.local`, `*.log`, `.DS_Store`).

Update this file once the project structure is established.
